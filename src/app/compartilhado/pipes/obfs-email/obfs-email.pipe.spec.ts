import { ObfsEmailPipe } from './obfs-email.pipe';

describe('ObfsEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new ObfsEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
